# Skubase growth operator

The mission is ten distinct qualified merchants acquired organically. The milestone counts verified store connections, useful analysis views or captured payments after mission start. Intent leads and historical activity remain visible separately. A reported Reddit membership attempt is retained as one unverified historical observation, never a paid customer.

## Runtime and recovery

The existing FastAPI service runs an opt-in background worker (`GROWTH_ENABLED=true`) against the existing SQLAlchemy database. Standalone alternative: initialize the product database, then run `python -m app.growth.worker`. Do not start an additional worker merely for scheduling. SQL compare-and-swap claims and fenced leases support recovery; ready work, evidence, outbox intents and cost reservations survive restarts. Periodic observes/inbox checks are five minutes, discovery is two bounded queries per six hours, and executive review is once per UTC day. Missed periodic work is superseded, not replayed in a research wave.

Transient failures retry at most three attempts with backoff. Configuration/policy failures remain blocked and visible. A send with an uncertain provider result is never automatically replayed, including after the provider's idempotency retention window. Reconcile its receipt before any manual intervention. Resend's provider idempotency window is [24 hours](https://resend.com/docs/dashboard/emails/idempotency-keys); the local outbox persists longer.

## Resource authority

Only environment configuration and authenticated owner operations grant authority. Model responses, public pages, replies and skill prose cannot enable spending, approve a contact, change the mailbox or execute code. Advertising has no execution adapter and remains disabled. Model/API budget defaults to zero; enable `GROWTH_MODEL_ENABLED` and set `GROWTH_DAILY_USD` only after an owner-approved ceiling. Reservations cover worst-case bounded tokens before network calls and remain charged when provider cost is uncertain. Routine planning, qualification and monitoring use deterministic code. Nano handles ambiguous routine classification; mini handles bounded analysis; Astra handles daily review or important uncertainty meeting the routing gate. Actual routing, tokens, latency and estimated cost are persisted. The owner can record evidence-linked comparisons at `POST /growth/usage/{id}/assessment`; routing incorporates the observed benefit after at least five comparable assessments. Escalation benefit remains unknown before those observations.

## Mailbox

The dedicated business inbox is `info@skubase.io`. It is not the owner's personal mailbox. `CONTACT_TO_EMAIL`, magic-link sender/reply-to and waitlist sender/reply-to use that address. Google Workspace Gmail is the primary inbox, with an info-only TLS delivery copy to Resend for the growth worker (verified below). A dedicated secret `GROWTH_RESEND_API_KEY` supports inbound reads and sends. The signed `/growth/webhooks/resend` endpoint uses `GROWTH_RESEND_WEBHOOK_SECRET`; five-minute inbox polling is a recovery path.

### Google Workspace migration — activated and verified 2026-09-07 UTC

- Google verified `skubase.io` and confirmed Gmail activation. The approved Business Starter **flexible** purchase is confirmed and active, with one license assigned to `info@skubase.io`: $8.40/month excluding tax, with paid service starting September 20, 2026. The initial Business Plus trial was replaced. `support@skubase.io` is a free alternate email on the same user; new app contact addresses remain `info@skubase.io`.
- Google Admin has an enabled routing rule `Copy info@skubase.io replies to the Skubase growth inbox` (rule `4ab9d`). It applies to inbound/internal-receiving mail for the exact envelope recipient `info@skubase.io` and keeps Gmail's normal delivery. Its additional delivery uses host `Skubase growth inbox`, `inbound-smtp.us-east-1.amazonaws.com:25`, with TLS, CA certificate and hostname validation required. Google validated the TLS connection. Spam is excluded from the copy and copy-recipient bounces are suppressed. This is [Gmail dual delivery](https://knowledge.workspace.google.com/admin/gmail/advanced/deliver-email-to-multiple-inboxes-with-dual-delivery); Resend [supports direct SMTP forwarding](https://resend.com/docs/dashboard/receiving/custom-domains).
- Apex SPF `v=spf1 include:_spf.google.com ~all` was added (record `rec_3d23f7c0d544720baaa7c4a0`). Existing Resend DKIM and the `send` subdomain's SPF/MX remain intact for application mail. Google's verification TXT is retained.
- The apex MX is now `1 smtp.google.com.` (record `rec_7ee8491e9686d8b540ecce07`), verified through the Vercel UI and public DNS. The earlier CLI update was rejected with `interactive_confirmation_required`; the prepared browser form was handed to the owner. On resumption, the saved Google MX was observed before continuing activation. No alternate API or delete/add workaround was used.
- Google's 2048-bit DKIM record `google._domainkey` was created (record `rec_6b3fc168299f36fee66e2376`). Although the earlier Admin form showed a waiting-period notice, the activated-domain setup successfully generated and verified the key. Admin now reports **Authenticating email with DKIM**. The previous waiting-period blocker is resolved.
- Internal setup message `70da0d26-f693-4f21-b98f-b63571f8cdb0` was delivered from Resend to Gmail and visually verified in the business inbox; its Resend dual-delivery receipt is `42ce49e5-168f-4f16-bb87-bd7442aee329`.
- A second internal message sent through Gmail appeared in the business inbox and produced Resend receipt `ec78cd75-934c-4a88-89ee-6c29dd6b2865`. Received authentication headers report `spf=pass` and `dkim=pass` for `skubase.io`. These tests use only the business mailbox and are excluded from merchant replies and acquisition metrics. They verify business mail delivery, not a customer response.
- Gmail is available at https://mail.google.com/mail/?authuser=info%40skubase.io. Optional content-personalization features were left off. DNS caches can take time to refresh; preserve the Resend receiving integration and Google dual-delivery rule so both delivery paths remain available during propagation.

`GROWTH_EMAIL_ENABLED` now enables narrowly requested-service fulfillment only, with working inbound and known provider marginal cost. No postal address is required or collected by this runtime. Five total service emails/day is the initial cap. A recent inventory-check request or a specific support question must authorize the exact unchanged response body. Unknown questions become owner obligations. Public business contact information and a positive reply alone do not authorize promotion. Resend [prohibits unsolicited cold outreach](https://resend.com/legal/acceptable-use), irrespective of a footer. The [FTC's commercial-email rules](https://www.ftc.gov/business-guidance/resources/can-spam-act-compliance-guide-business) explain the postal requirement and distinguish narrowly transactional/service messages; a home address is not required. This is not a workaround to relabel marketing as support. There are no silence followups. Refusals, opt-outs, complaints and delivery failures suppress future sends. Contact forms reach `info@skubase.io` independently of service automation.

The `requested_service` skill records the source knowledge, version and observed outcome. Deterministic responses cover connection help, read-only behavior and credential-sharing precautions. The outbox requires a retained request from the preceding 14 days and a matching body hash; model prose or owner-added contact facts cannot bypass this requirement. Questions beyond those verified answers remain visible in the owner dashboard.

## Daily executive review without a separate model API

Set `GROWTH_REVIEW_MODE=codex`. The server prepares one bounded evidence packet per UTC day. The **Skubase daily growth review** Codex heartbeat runs at 9 a.m. local time using the existing subscription allowance, with at most two decision-directed public source reads. It does not require a new OpenAI API key or paid API budget. Codex/local host availability is required; the Railway worker continues independently if the desktop is unavailable. Last-completed review time is visible on the dashboard. Subscription cost allocation and unavailable token counts remain UNKNOWN, rather than being reported as free compute.

`scripts/growth-review.ps1 -Action review-export` verifies the linked Skubase Railway project, connects through its existing authorized database access and returns at most 16 evidence excerpts plus compact strategy/funnel/beliefs. It returns a cheap `already_reviewed` receipt after completion. Write the review JSON to an ignored local file, then run `scripts/growth-review.ps1 -Action review-import -File <absolute-path> -Model <actual-model-or-codex/current-thread>`. Every required field, evidence ID and action is validated. Importing twice creates neither a duplicate review nor duplicate usage. The importer persists the strategy and chooses a bounded discovery focus or queues evaluation/product feedback. It cannot grant spending, message authority, arbitrary fetch URLs or code execution.

Discovery shares a persisted two-query cap per six-hour window across strategy changes and restarts. Deeper topic research has a separate two-fetch cap shared by all entry points, verifies the exact observed author/post and does not crawl additional links. Vendor promotion stays separate from merchant evidence. Community responses are only drafted where the generic answer fits the observed problem; posting requires a configured business identity and verified channel rules. Failed or blocked public reads never trigger proxy or account bypasses.

## Owner operations

Sign in as an existing admin and open `/growth`. The authenticated HTTP API offers evidence pagination, pause/resume, safe retries of blocked draft work, qualification with fact sources, and skill revision activation/rollback. Admin identity comes from existing authentication; no new public admin token. `/growth/events` accepts a constrained list of diagnostic events, never client assertions of purchase or store connection. Analysis views require an authenticated browser and recent usable data delivery for the same store. Demo/admin activity is excluded. Attribution persists for 30 days in first-party storage, respects Do Not Track, and excludes URL query data except named UTM fields.

`POST /growth/product-review` releases an acquisition hold with retained verification evidence and a written resolution. Contact-form submissions are delivered to `info@skubase.io` and recorded as support obligations; they do not grant outreach consent. PostgreSQL schema creation shares a transaction advisory lock between API startup and standalone initialization to avoid concurrent DDL races. See [PostgreSQL advisory locks](https://www.postgresql.org/docs/16/explicit-locking.html#ADVISORY-LOCKS).

Memory is divided into working, strategic, customer, channel, attribution, economics and beliefs; raw evidence/contact/message/experiment/usage tables stay separate. Model context retrieves a maximum of 12 recent relevant records by default rather than the whole history. Skill JSON declares purpose, inputs, tools, process, output, success, failure and expected cost. Proposed versions require cited evidence and validation; owner activation is reversible and never edits runtime code.

## Measurement limits and development workflow

Shopify subscription activation can include a free trial. It is not payment evidence. Signed live Stripe invoice receipts support captured-payment events and recurring amounts when supplied. The optional read-only Shopify Partner adapter observes `AppSubscriptionSale` receipts for the exact configured app, matches the Shopify store, excludes test/admin stores, and deduplicates actual positive-amount receipts. It reads one bounded 50-row page per hour with a durable cursor and overlap. Configure `SHOPIFY_PARTNER_ORGANIZATION_ID`, `SHOPIFY_PARTNER_APP_ID` and `SHOPIFY_PARTNER_API_TOKEN` to activate it; no such token is currently configured in production. Collected amounts may be prorated or discounted, so the adapter does not infer contract MRR from receipts. CAC, retention and LTV remain unknown without sufficient attributed billing evidence. Raw first-party visits are unverified diagnostics and must not drive customer claims.

Product feedback is evidence plus a hypothesis and recommended action; downstream bottlenecks hold new outbound. Product changes require the existing development/review workflow. Public opportunity discovery currently supports bounded Shopify Community reads and attributable evidence imports; it does not automate Reddit posting, resolve every merchant's private contact identity, or bypass channel restrictions. These are explicit capability limits.

## Verification

Run `python -m unittest discover -s tests` from `backend`; `npm run build` and `npm run typecheck` from `frontend`. `tests/test_growth.py` exercises an isolated simulated full loop: opportunity → selected experiment/draft → provider receipt → restart → human reply → evidence/experiment update → changed future allocation → next assistance action. It also covers leases, duplicate prevention, suppression, ambiguous sends, spend ceilings, hostile source data, skill rollback and authorization. These fixtures are not customer acquisition evidence. `tests/growth_preview.py` provides a localhost-only visual fixture and must never be used as a production entry point.

After deployment, explicitly run `python -m app.growth.seed_live` once to retain attributed public market observations, counterevidence and the first calculator experiment. Re-running is idempotent. The calculator's success metric is an attributed connected store, not calculator use. Publishing the calculator and reading public evidence are real permitted actions; a real merchant response and ten qualified users are still outcomes to acquire, not implementation test results.

## Live verification — 2026-09-07 UTC

- Backend: Railway deployment `1642272f-a616-4dba-869c-df849c327580`, healthy. Frontend: Vercel deployment `dpl_CxXNgVAmnivfKMjHb88kFV7fmizj`, ready on `www.skubase.io`.
- Dedicated mailbox outgoing setup receipt `764f913d-3c63-46c5-8aab-4977c6f69e78` was delivered; incoming setup receipt `1213aaa2-05d9-4bc6-ba7b-4598d4e81406` confirms receiving.
- The real `/contact/submit` endpoint acknowledged the internal setup submission. Its received-mail receipt is `0173e307-3907-409f-a63c-86e2d09ed10b`. Signed delivery/receiving webhooks returned HTTP 200. Setup emails are excluded from merchant replies.
- The deployed worker completed two public research jobs (20 and 15 results examined). It reconciled 18 historical signups and 12 historical store connections. These are baseline records, not new mission customers or verified paid customers.
- Mission identity started at epoch `1788745066.7557864` and survived redeployment unchanged. The deployed process completed queued inbox and observation checks after restart. Model/API runtime spend and advertising spend remain zero; new qualified users remain zero.
- The first live organic experiment is the [reorder point calculator](https://www.skubase.io/tools/reorder-point-calculator), linked from the existing safety-stock article. Public research included [a purchasing/forecast-trust question](https://www.reddit.com/r/AutomateShopify/comments/1w0sta4/does_anyone_actually_let_inventory_planning/), [an inventory-adjustment workflow request](https://community.shopify.com/t/looking-for-inventory-adjustment-alternatives/639382), and [a high-SKU merchant counterexample](https://community.shopify.com/t/stocky-alternative-recommendation/553677/42). These are three selected observations, not a representative survey.
- A raw urllib mailbox transport error was corrected using the provider-compatible requests transport with explicit Skubase identity. Its failed attempts remain in the audit trail. Historical missing activation telemetry was also corrected: it cannot establish a product failure. The previous hold was released with evidence.
- 75 backend tests, 25 frontend tests, TypeScript checks and the production Next.js build passed. The dashboard/calculator were visually checked in a local isolated fixture; production confirms the public calculator and protected owner dashboard. The full response-driven learning loop was verified with simulated merchant replies, not a fabricated live customer response.
- The original activation notes requiring a mailing address/API key were superseded by the requested-service dispatcher and subscribed Codex executive bridge described above. Promotional email remains disabled. Shopify receipt collection still needs read-only Partner access; community posting still needs a business identity and channel review. Neither is a reason to stop routine research, service obligations or learning.

### Follow-on deployment — 2026-09-07 UTC

- Railway deployment `a50c0a29-e57d-439f-9919-2fb41bd43422` is SUCCESS; Vercel deployment `dpl_8QQMpqFaEsCDiTf1koeRfiYFDfe4` is READY on `www.skubase.io`. Backend health returns 200 and the unauthenticated dashboard API returns 401. The immediate deployment-log scan found no application errors; this is a bounded observation, not a monitoring guarantee.
- Production now has `GROWTH_EMAIL_ENABLED=true`, `GROWTH_REVIEW_MODE=codex`, `GROWTH_DAILY_USD=0` and no mailing address configured. The dispatcher accepts only recent, unchanged requested-service responses. No real merchant email was sent during this follow-on deployment.
- Executive review evidence `307` changed discovery focus from broad Stocky results to first-person merchant problems. A second export returned `already_reviewed=true`. Automation `skubase-daily-growth-review` is ACTIVE, scheduled at 9 a.m. local time. Its host and Codex must be available for the review; the server worker runs independently.
- The worker resumed observation and inbox work, retired the old unsent cold-email experiment as inconclusive, created a separate requested-health-check experiment, and enforced the shared research cap after restart. Identity start remains `1788745066.7557864`; the new mission cohort remains 0/10.
- 80 backend tests and 25 frontend tests passed, plus TypeScript and the production Next.js build. The growth-specific suite has 19 tests, including exact-body service authorization without an address, suppression through contact forms, once-daily cited strategy import, source-post matching/research caps, and receipt deduplication without invented MRR. Merchant responses remain simulated fixtures, clearly separate from live acquisition evidence.
- Remaining capability dependencies are read-only Shopify Partner finance access and a permitted business community identity. No personal mailing address or separate paid model API is needed for the active research, requested-service and daily-review paths.
